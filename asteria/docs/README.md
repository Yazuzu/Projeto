# Astéria

Projeto AI Vtuber Open Source.

## Como iniciar

1. Configure o arquivo `.env` com seu token do Discord.
2. Instale as dependências via `pip install -r requirements.txt`.
3. Rode `python bot/bot.py`.
